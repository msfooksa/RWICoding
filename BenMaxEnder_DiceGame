# Imports The rabdom module
import random

#Sets lists and variables
w=int(input("How many players are playing this round?: "))
a=int(input("How many rounds would you like to play?: "))
Name={}
points = {}

#Sets the variables in the list
for i in range(w):
    Name[input("Enter your name:")] = input("Guess the sum of three randomly rolled dice:")


#Creates the For loops
for i in Name.keys():
    points[i] = 0
    for x in range (a):
    
#Sets dice values for the round
        die1= random.randint (1,6)
        die2= random.randint (1,6)
        die3= random.randint (1,6)
        final_number = die1 + die2 + die3
        print("The sum of the dice is " + str(final_number))
#Checks to see if you got it right
        if int(Name[i]) == final_number:
            points[i] += 1
        else:
            points[i] += 0
  
#Calculates the winner and prints it out
winner = max(Name.items())
print("The winner is " + str(winner))
print(points)

=======
#import random lets the program choose a random number 
import random
#this makes it where the numbers chose randomly are only between 1 and 6 
random.randint (1,6)

#creates three different die and makes them only have a number between 1 and 6
die1= random.randint (1,6)
die2= random.randint (1,6)
die3= random.randint (1,6)
#makes final number the sum of all three die 
final_number = die1 + die2 + die3

#asks user for round count and input is the number used 
a=int(input("How many rounds would you like to play?: "))
#asks for players in the round and takes their names and guesses and repeats this process for how many rounds entered r
for i in range (a):
    w=int(input("How many players are playing this round?: "))
    print("There are three die with the numbers 1-6 on each one. Pick a number between 1 and 18 and if you guess the same number as the sum of all three dice than you win.")
    names_and_guesses={}
    for i in range (w):
        name=input("What is your name, player?: ")
        guess=int(input("What is your guess this round?: "))
        (names_and_guesses[name])=guess
    die1= random.randint (1,6)
    die2= random.randint (1,6)
    die3= random.randint (1,6)
    final_number = die1 + die2 + die3
    print(names_and_guesses)
#prints the dice roll amount
    print("The sum of the dice is " + str(final_number))
#creates if statememt telling each user if they are right or wrong 
    if final_number in names_and_guesses.values():
        for i in names_and_guesses.keys():
            if final_number == names_and_guesses[i]:
                print("You guessed correct! Those are some slim odds Bucko!")
            else:
                print("You guessed wrong. Better luck next time.")
#if all users are wrong it prints this
    if final_number != names_and_guesses.values():
        print("You are all wrong!!")
                
# michaela's feedback:
# 1. please comment your code in the format we've discussed in class
# 2. you only print the "you guessed wrong" line if someone guessed correctly - this should be printed regardless
# 3. print who guessed correct - rather than saying "you guessed correct" if any one of the players guessed correctly, it should read, say, "max guessed correct"
# 4. all players are playing all rounds. declare an overall winner based on who guessed correctly the most number of times
